{
  "name": "select-sideline-blog",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "astro": "^7.2.7"
  },
  "devDependencies": {
    "wrangler": "^4.126.0"
  },
  "overrides": {
    "esbuild": "^0.28.1",
    "sharp": "^0.35.0"
  }
}
